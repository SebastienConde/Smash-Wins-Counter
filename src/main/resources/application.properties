# SmashWinsCounter application configuration

# Use environment variables if set; otherwise fallback to local defaults
#spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://<URL_Here>?sslmode=require}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME:<USERNAME_HERE>}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:<PASSWORD_HERE>}

# Hibernate / JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Application name
spring.application.name=smashwinscounter
